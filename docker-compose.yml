services:
  jellyfin:
    image: jellyfin/jellyfin:10.11.11 # docs/jellyfin-openapi.json is this version's spec (slice 019)
    container_name: jellyfin
    ports:
      - "8096:8096/tcp"
      - "7359:7359/udp"
    volumes:
      - jellyfin-config:/config
      - jellyfin-cache:/cache
      - type: bind
        source: ./media
        target: /media
        read_only: true
    restart: unless-stopped

volumes:
  jellyfin-config:
  jellyfin-cache:
